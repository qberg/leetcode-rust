pub struct Solution {}

impl Solution {
    pub fn close_strings(word1: String, word2: String) -> bool {
        match word1.chars().count().cmp(&word2.chars.count()) {
            std::cmp::Ordering::Equal => {

            },
            _ => false,
            _ => false,
        }
    }
}
